import Foundation
@testable import PrintFarmer

// MARK: - Test-only Protocol
// AuthServiceProtocol is now defined in production code at
// PrintFarmer/Protocols/AuthServiceProtocol.swift.

// NOTE: PrinterServiceProtocol, JobServiceProtocol, NotificationServiceProtocol,
// StatisticsServiceProtocol, and AuthServiceProtocol are now defined in production code
// at PrintFarmer/Protocols/. Do NOT redefine them here.
